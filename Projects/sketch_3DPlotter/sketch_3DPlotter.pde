float[][] points;

//distance between adjacent points on point grid
float dist = 1;

void setup(){
  size(800, 800);
  
  int val =  (int) (800 / dist);
  points = new float[val][val];
}

void draw(){
  
}
